name: Changeset
on:
  pull_request:
    types: [labeled]
  workflow_dispatch:
jobs:
  changeset:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Changeset
        uses: ./
